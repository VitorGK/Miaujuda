import Foundation

enum UrlRoute: String {
    case user = "/user/"
    case petPost = "/petpost/"
    case latestPetPost = "/petpost/latest/"
    case item = "/item/"
    case auth = "/auth/login/"
}
